@import shared/commonElements.gspec

@objects
  usernameInput #username
  passwordInput #password
  submit        #login
  footer		#info
  timeline 		#timeline

# concrete layout tests

= Login form visible =
  usernameInput:
    visible
  passwordInput:
   visible
  submit:
   visible
  footer:
   height 20 to 63px
   width 100 to 1024px
  timeline:
   visible
